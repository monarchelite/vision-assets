import { createHash } from 'crypto';

export function generateHash(filePath: string) {
  // Use file path + timestamp to create a short hash
  return createHash('md5')
    .update(filePath + Date.now().toString())
    .digest('hex')
    .slice(0, 8); // 8-character hash
}
